import 'package:flutter/material.dart';

class PenggunaTab extends StatelessWidget {
  const PenggunaTab({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Halaman Pengguna", style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold)));
  }
}