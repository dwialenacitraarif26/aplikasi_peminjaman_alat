import 'package:flutter/material.dart';

class AppColors {
  // Warna Utama Aplikasi (SIMBARA)
  static const Color darkblue = Color (0xFF091F5B);
  static const Color primary = Color(0xFF344EAD); 
  static const Color inputBg = Color(0xFFD0E4FF); 
  static const Color white = Colors.white;
  static const Color grey = Colors.grey;

  // Warna Berdasarkan Role (Sesuai permintaanmu)
  static const Color admin = Color(0xFFD32F2F);    // Merah
  static const Color petugas = Color(0xFF1976D2);  // Biru
  static const Color peminjam = Color(0xFF388E3C); // Hijau

  // Warna Tambahan
  static const Color error = Colors.redAccent;
}